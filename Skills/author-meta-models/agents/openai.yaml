interface:
  display_name: "Author Meta Models"
  short_description: "Use the owning CLI to author sanctioned models"
  default_prompt: "Use $author-meta-models to select the owning CLI and author this workspace through supported commands."
