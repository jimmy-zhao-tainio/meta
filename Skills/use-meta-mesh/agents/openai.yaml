interface:
  display_name: "Use MetaMesh"
  short_description: "Compose and run modeled multi-workspace operations"
  default_prompt: "Use $use-meta-mesh to compose, inspect, validate, or run this modeled multi-workspace workflow."
