interface:
  display_name: "Use Meta Transform Flow"
  short_description: "Operate transforms from schema through orchestration"
  default_prompt: "Use $use-meta-transform-flow to carry this transform through the correct Meta-BI semantic boundaries."
