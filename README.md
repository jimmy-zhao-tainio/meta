# meta

Model-first metadata tooling for deterministic XML, SQL, and C# representations.

The public reference is generated from authored MetaDocs workspaces:

- `MetaDocs\Docs\Site\docs.html`
- `MetaDocs\Docs\Documentation.MetaMesh`

Use `meta help` for local command help and `meta-docs browse` for the modeled reference.
